const Loading = () => {
    return (
        <div className="flex justify-center align-middle flex-grow">
        <span className="loading loading-spinner loading-lg"></span>
        </div>
    );
}

export default Loading;